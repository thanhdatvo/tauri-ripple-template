# Tauri + RippleTS + TypeScript

This template should help you get started developing with Tauri, RippleTS and TypeScript in Vite.

# How to start

Step 1: Clone the repository

Step 2: run this command to install dependencies:

```bash
bun install
```

Step 3: start developing your desktop app

```bash
bun tauri dev
```
